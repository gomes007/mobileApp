import {StyleSheet} from "react-native";

const styles = StyleSheet.create({
    container: {
        height: 51,
        backgroundColor: '#5BD075',
        justifyContent: 'center',
        alignItems: "center",
        borderRadius: 8
    },
    buttonText: {
        color: 'white',
        fontSize: 16,
        fontWeight: "600"
    }
});
module.exports = styles;
